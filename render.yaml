services:
  - type: web
    name: cryptoflashr
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
```

### **Step 2: Verify `public/_redirects`**
```
/*    /index.html   200